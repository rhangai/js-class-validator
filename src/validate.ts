// import { metadataGetValidator } from './Metadata';

export async function validate(classType: any, obj: any) {
	// const validator = metadataGetValidator(classType.prototype);
	// const instance = await plainToClass(classType, obj);
	// console.log(validator);
}
